# The same job and the same data on two workers. One cell of the matrix: what
# changes between this file and its neighbours is the fleet, and nothing else.
mode: scaled
seed: 11
job: Elastic
scale: 6

machines:
  master: { instance: m5.2xlarge, zone: eu-central-1a }
  workers:
    count: 2
    prefix: w
    instance: r5.large
    zone: eu-central-1a
    runs: [Combiner]


# What each rpc costs on the reference machine, under the class that serves it.
# The handlers declare nothing: a duration is a claim about the machine a design
# would run on, so it belongs to the scenario — and a system stays plain gRPC
# with no losim in it.
takes:
  Combiner:
    Map: { refMs: 2, refNsPerRecord: 360000 }
    Reduce: { refMs: 5 }
