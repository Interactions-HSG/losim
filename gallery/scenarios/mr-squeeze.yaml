# Short of disk and short of memory at the same time, on different machines.
#
# One machine per line, because the failures are not interchangeable and this is
# the scenario that shows they are not. m1 runs out of volume in phase (6) and
# fails a map task that has to be placed elsewhere. s0 runs out of memory in phase
# (8) and simply stops existing. r2 cannot write its output in phase (10).
#
# Three machines, three resources, three completely different consequences — and
# only one of them leaves the fleet smaller than it started.

seed: 27
kTime: 20
job: MapReduce
expectedRun: 60 refSeconds

workload: { records: 10000 }

machines:
  master: { instance: m5.large, zone: eu-central-1a, serves: [Watchtower] }

  m0: { instance: c5.large, zone: eu-central-1a, serves: [MapMachine, Beacon], diskMb: 8 }
  m1: { instance: c5.large, zone: eu-central-1b, serves: [MapMachine, Beacon], diskMb: 0.5 }
  m2: { instance: c5.large, zone: eu-central-1c, serves: [MapMachine, Beacon], diskMb: 8 }

  s0: { instance: r5.large, zone: eu-central-1a, serves: [ShuffleMachine, Beacon], memoryMb: 80 }
  s1: { instance: r5.large, zone: eu-central-1b, serves: [ShuffleMachine, Beacon], memoryMb: 900 }
  s2: { instance: r5.large, zone: eu-central-1c, serves: [ShuffleMachine, Beacon], memoryMb: 900 }

  r0: { instance: r5.large, zone: eu-central-1a, serves: [ReduceMachine, Beacon] }
  r1: { instance: r5.large, zone: eu-central-1b, serves: [ReduceMachine, Beacon] }
  r2: { instance: r5.large, zone: eu-central-1c, serves: [ReduceMachine, Beacon], diskMb: 0.01 }

network: { sameZone: 1 refMs, crossZone: 12 refMs, jitter: 2 refMs }
