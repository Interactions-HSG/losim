/** The row lights its own cells: `.rollup tr:hover td`, without a descendant selector. */
import * as stylex from '@stylexjs/stylex';

export const rollupVars = stylex.defineVars({ cellBg: 'transparent' });
