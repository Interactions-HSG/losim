---
title: "losim diff"
description: "Whether two traces are the same simulator — not which design is better."
icon: "code-compare"
---

```bash
java -cp build/losim.jar losim.cli.Main diff <a.json> <b.json>
```

<Warning>
This answers **"are these two the same simulator?"** It is the tool for checking
that a laptop, the devcontainer and a Codespace produce the same thing, and it is
what CI uses. It is *not* for comparing two designs — for that,
see [comparing designs](/run/comparing-designs).
</Warning>

## A textual diff would be useless

Runs are not reproducible, on purpose: real threads, a real wall clock, and a
deliberate refusal to simulate a scheduler. Two runs of one scenario on one machine
differ in every duration and most byte counts. A textual diff could never pass, and
a check that can never pass is not a check.

So everything compared is split in two.

## Structural — must agree

A difference here is a **defect**: the two environments are not running the same
simulator.

| | |
|---|---|
| `schema` | the trace schema version |
| `meta.scenario`, `job`, `seed`, `kTime`, `telemetry`, `mode`, `schemaVersion`, `trusted`, `completed` | |
| what each **event kind carries** | the keys each kind's `detail` holds |
| what each **span kind is labelled** | |
| sampled channels | which `<vm>.<metric>` channels exist |
| machines | which machines existed |
| **what each resource is a function of** | the attribution — a fact about your program, which must not depend on where it ran |
| what the engine refused | |

<Note>
Event kinds are compared on **what each carries**, not on which kinds occurred. A
`queue_wait` appears when a call had to queue, and on a busier host more calls
queue — that is the simulator working, not two simulators disagreeing.
</Note>

## Measured — printed, not judged

| | |
|---|---|
| `meta.durationRefMs` | |
| the run size the engine chose | |
| every fitted exponent | |

A Codespace is slower than a laptop, and that is what host calibration is for.

## Output

```
build/parity/traces-runner/t10.json
build/parity/traces-codespace/t10.json

  same    schema
  same    meta.scenario
  same    meta.kTime
  same    what each event kind carries
  same    what each span kind is labelled
  same    sampled channels
  same    machines
  same    what each resource is a function of
  same    what the engine refused
  differs meta.durationRefMs               4820  vs  6113
  differs run size chosen                  8000  vs  8000
  differs exponent of memoryMb            1.013  vs  1.011
```

Exit code is non-zero when a **structural** aspect differs.
