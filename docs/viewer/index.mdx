---
title: "The viewer"
description: "Inspect a run over time, including system state and cost."
icon: "eye"
---

A trace is a collection of JSON objects. The viewer opens **a raw `losim simulate`
trace** directly, without a preprocessing step.

## Try it

In a Codespace or devcontainer, the lab server starts at **http://localhost:8000/**.
The picker lists runs under `build/results` that exist on disk.

To choose runs explicitly, `show()` opens the viewer on the runs it created:

```java RunExperiments.java
Experiments.here()
        .run("main.yaml")
        .show();
```

<Note>
**Every example in this manual makes its own trace.** The pages do not depend on a
shared set of prepared runs.
</Note>

<Note>
The page is a committed static export served by the JDK. It works in the devcontainer,
on a laptop, and in a Codespace.

`npm` belongs to whoever *changes* the viewer. [Developing it ->](/viewer/developing)
</Note>


## On screen

The cards below describe the viewer's main views. Spans and Topology are switches
inside Film; the ledger is both a Film panel and the Cost page.
[View details ->](/viewer/views)

<Columns cols={2}>
  <Card title="The film" icon="film">
    The system at an instant. Zones are bands, roles are columns, and a node is
    drawn **to scale**: memory changes its width and cores change its height.

    Each node shows **remaining capacity**, not only current usage. The remaining
    memory determines how close it is to an out-of-memory failure.
  </Card>
  <Card title="The scrubber" icon="sliders">
    Its **chapters are the run's phases**, and its **markers are recorded failures**:
    kills, freezes, out-of-memory events, disk-full events, timeouts, and retries.
  </Card>
  <Card title="Spans" icon="bars-staggered">
    *Inside Film, and on Overview.* The distributed call stack as a waterfall,
    swimlanes, and a rollup. It shows where time was spent and where callers waited.
  </Card>
  <Card title="Topology" icon="circle-nodes">
    *Inside Film.* The system shape, with edges weighted by **bytes** rather than call
    count.
  </Card>
  <Card title="The node panel" icon="rectangle-list">
    Hover to sample and pin to **watch** a node while the trace plays.
  </Card>
  <Card title="The ledger" icon="receipt">
    *The Cost page and a panel over the film.* The buckets accrue **while the film
    plays**, and a node hover shows its share.
  </Card>
</Columns>

[View details ->](/viewer/views)

## The clock is in reference milliseconds

<Note>
The scrubber reads the clock **used by the simulation**, not the host's compressed
clock. It therefore matches the durations written in YAML.
</Note>

`1x` means one simulated second per real second, everywhere in the console. The
playhead moves through trace time at a constant rate:

| | |
|---|---|
| **monotone** | trace time never goes backwards, so nothing is reordered and nothing overlaps that did not overlap |
| **read at the true instant** | every number on screen is taken from the trace at the trace time the playhead is at — a node's memory at 400 refMs is what it was at 400 refMs |
| **the scrubber is still refMs** | the axis is unchanged, and the playhead crosses it evenly |

<Tip>
**A run whose calls are quicker than a video frame is a run to slow down.** The
rates go to `0.001x`, where one reference millisecond takes a second on screen —
a control message that took three refMs crosses the gap in three seconds and can
be hovered while it does. The [waterfall](/viewer/views#spans) answers the same
question the other way: there, a three-refMs call is a bar, whatever the clock is
doing.
</Tip>

### The `fit` rate

`fit` plays **the whole run in thirty seconds**, whatever it took — a 5.2s run
slowed by six, a 165s run sped up by five. Use it for an overview and a numbered
rate for detailed reading. Pressing a numbered rate clears `fit`.


## Status

The console has a rail of pages. **Runs** is the gallery. **Simulations** lists,
creates, edits, and runs simulations. **Overview**, **Film**, **Usage**, and **Cost**
read one trace and share its clock. **Spans** and **Topology** are sub-views inside
Film. [What each one shows ->](/viewer/views)

Two runs can be watched **side by side on one clock**. Pick a second from
`compare with…`; the viewer shows their relative cost below the films.
[How that works ->](/viewer/views#two-runs-on-one-clock)


## Film and video export

An earlier Python prototype rendered a run as a **video file**. It was replaced
because a video cannot be hovered, scrubbed, or queried.

The current viewer keeps playback and adds interactive views. Each element is a pure
function of its frame, so **scrubbing, playing, and recording share one code path**.

The Python modules remain the **reference implementation** for the port, checked glyph by
glyph and layout by layout. The video renderer is not one of them.
[How that is checked ->](/viewer/developing)

<Note>
The recorder's MP4 follows the film. Because the clock is an encoder input, a slow
laptop produces the same timing as a fast one, but takes longer.
</Note>

## Stack

| | |
|---|---|
| **Next 16** + **React 19** | `output: 'export'` — a static site, no server |
| **d3 7** | scales, timers, geometry |
| **mediabunny** | MP4 export through WebCodecs |

The viewer does not use elkjs, Radix, framer-motion, or react-virtual. It fetches
nothing at runtime, so it does not depend on network availability.

<CardGroup cols={2}>
  <Card title="Opening a trace" icon="folder-open" href="/viewer/opening-a-trace">
    `build/results` is the place to point a run at.
  </Card>
  <Card title="The views" icon="table-columns" href="/viewer/views">
    The film, scrubber, panel, ledger, and keys.
  </Card>
  <Card title="Viewer design" icon="wand-magic-sparkles" href="/viewer/viewer-design">
    Practices your job can use to change the picture.
  </Card>
  <Card title="Developing the viewer" icon="code" href="/viewer/developing">
    building the export, the dev server, and why the export is committed.
  </Card>
</CardGroup>
