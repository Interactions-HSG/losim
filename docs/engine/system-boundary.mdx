---
title: "System boundary"
description: "The boundary between the program and dissaly's simulation model."
icon: "layer-group"
---

```
┌─────────────────────────────────────────────────────────────┐
│  dissaly                                                       │
│    ClientSide + ServerSide interceptors on every call        │
│    a node model that caps memory and disk                 │
│    a compressed clock                                        │
│    a scaler engine, a verifier, a bill                       │
├─────────────────────────────────────────────────────────────┤
│  your system                                                 │
│    real .proto, real generated stubs, real marshaling       │
│    real threads, real allocation, real contention            │
└─────────────────────────────────────────────────────────────┘
                    one JVM, one laptop
```

<Note>
The simulation uses no ports, containers, simulated scheduler, or virtual clock.
Nodes run on real threads against a real wall clock. dissaly shapes the run through
**gRPC's own extension points** with an in-process server and executor for each
node, sized to its vCPUs, plus an interceptor on each side of every call.
</Note>

## Separation boundaries

<Columns cols={2}>
  <Card title="Failure behavior" icon="fire">
    "Runs out of memory" describes a real limit. The node received its configured
    capacity, and application code exceeded it.
  </Card>
  <Card title="Portable service code" icon="box-open">
    No dissaly type appears in a service signature. The handler remains an ordinary
    object with an ordinary method.
  </Card>
</Columns>

## The public package

`dissaly.api` is the public package. It depends on no runtime type:

| | |
|---|---|
| `Dissaly` | the static entry point |
| `DissalyCtx` | what you may say to dissaly from inside a handler |
| `Spec` | what **this** node is made of |

The public surface is limited to these types. `dissaly.pb` is protoc's generated
output for `dissaly/job.proto`, not part of the API. `Job` is therefore not listed
here.

`dissaly.runtime`, `dissaly.trace`, `dissaly.scale`, `dissaly.verify`, and the remaining
packages are internal. Code that depends on them is tied to the simulator.

## Two implementations

`Dissaly.current()` returns one of two singletons:

| | inside a run | outside one |
|---|---|---|
| recording: `reveal`, `log`, `units`, `sleep` | reach the node | **silent** |
| state: `node`, `here`, `peers`, `clockMs`, `channelTo` | reach the node | **throw** |

A silent `reveal` lets a handler run directly in a test. A **fabricated** system
would let the test pass without checking the interaction.

## Node context

The node is not passed as a method parameter. gRPC's `Context` carries it: the
server interceptor attaches the node with `Contexts.interceptCall`, making it
ambient for work performed on that thread.

<Warning>
The context follows work only onto threads created by the node. Wrap the node's
executor once with `Context.currentContextExecutor`; otherwise the work loses its
node identity and the verifier flags it without attribution.
</Warning>

## In-process gRPC

In-process transport retains the gRPC stubs, marshaling, interceptors, status
codes, deadlines, and metadata. The transport uses no real *sockets*.

<Note>
**The in-process transport passes object references.** It hands the receiver the
same object reference. Protobuf messages are immutable, so passing by reference is
safe. The transport cannot provide a byte count, so dissaly marshals the message
explicitly and adds the five bytes of framing that gRPC would have used.
</Note>

## Package map

| | |
|---|---|
| `dissaly.api` | the public facade available to a program |
| `dissaly.runtime` | the system, nodes, and interceptors |
| `dissaly.trace` | the three-channel recorder and the trace it writes |
| `dissaly.time` | the compressed clock and fault placement |
| `dissaly.res` | instance types, heap walks, and dissaly's own meter |
| `dissaly.scale` | the probe grid, fitted laws, solves, and refusals |
| `dissaly.sim` | a system and its failure conditions as data |
| `dissaly.verify` | checks for measurements that lose their meaning |
| `dissaly.price` | billing buckets and excluded quantities |
| `dissaly.cli` | `simulate`, `bill`, `compare` |
